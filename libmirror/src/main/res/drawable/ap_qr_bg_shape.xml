<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- rectangle表示为矩形 -->

    <!-- 填充的颜色 -->
    <!--    <solid android:color="@color/homeDive" />-->
    <gradient
        android:angle="0"
        android:startColor="#ffffff"
        android:endColor="#ffffff"
        />

    <!-- 边框的颜色和粗细 -->
    <!--    <stroke
            android:width="1dp"
            android:color="@color/homeDive"
            />-->

    <!-- android:radius 圆角的半径 -->
    <corners
        android:radius="@dimen/px_positive_20"
        />

</shape>